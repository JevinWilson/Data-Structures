#include <gtest/gtest.h>
#include <hash_table.h>
#include <graph.h>

int main()
{
    /*
    testing::InitGoogleTest();
    return RUN_ALL_TESTS();*/
    

    /*ssuds::Graph<std::string, float> graph;

    graph.add_node("D");
    graph.add_node("A");
    graph.add_node("G");
    graph.add_node("B");
    graph.add_node("R");
    graph.add_node("F");

    graph.add_edge("A", "B", 0.6);
    graph.add_edge("A", "F", 1.3);
    graph.add_edge("G", "B", 0.1);
    graph.add_edge("B", "R", 2.5);
    graph.add_edge("B", "G", 0.1);
    graph.add_edge("R", "G", 4.0);
    graph.add_edge("F", "D", 3.7);
    graph.add_edge("F", "G", 2.8);
    graph.add_edge("F", "A", 1.3);

    std::cout << graph << std::endl;

    return 0;*/

    
    
}